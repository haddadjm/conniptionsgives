<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.conniptionsgives">

    <application
        android:allowBackup="true"
        android:icon="@drawable/hawkwood"
        android:label="@string/app_name"
        android:roundIcon="@drawable/hawkwood"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".YouDiedVideo"></activity>
        <activity android:name=".GetParried" />
        <activity android:name=".YouDied" />
        <activity android:name=".TimeForAToast" />
        <activity android:name=".PraiseIt" />
        <activity android:name=".GiveConniptions" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>